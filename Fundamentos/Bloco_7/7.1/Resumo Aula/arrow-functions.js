// const completeName = (name, lastName) => name + lastName;
// console.log(completeName('Olavio', ' Timoteo'));

// const userInfo = (nome, idade) => ({ nome: nome, idade: idade });

// console.log(userInfo('Olavio Timoteo', 23));

const printName = function(name) {
  const myName = name;
  return myName;
}
console.log(printName('Olavio'));
